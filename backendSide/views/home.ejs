<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'منصة رقمي' %></title>
    <link rel="stylesheet" href="/styles/main.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="font-sans bg-white text-gray-900">
    <!-- مثال على إدخال بيانات من السيرفر -->
    <% if (message) { %>
      <div class="text-center bg-green-100 text-green-800 p-4">
        <%= message %>
      </div>
    <% } %>

    <!-- يمكنك هنا استبدال الأقسام بـ includes إذا كنت تستخدم partials -->
    <% include partials/navbar %>

    <!-- المحتوى الرئيسي -->
    <main>
      <!-- مثال: منتج -->
      <% products.forEach(product => { %>
        <div class="product-card">
          <img src="<%= product.image %>" alt="<%= product.name %>" />
          <h3><%= product.name %></h3>
          <p><%= product.price %> ر.س</p>
          <form action="/cart/add" method="POST">
            <input type="hidden" name="productId" value="<%= product.id %>">
            <button type="submit">اضف للسلة</button>
          </form>
        </div>
      <% }); %>
    </main>

    <% include partials/footer %>

    <script src="/script.js"></script>
  </body>
</html>
